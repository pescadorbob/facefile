**/node_modules/**
e2e/playwright-report/trace/**
e2e/test-results/**
e2e/playwright-report/**
backend/**/*.db

# Amplify Gen 2 — generated per sandbox/deploy, not checked in
amplify_outputs.json
amplify/.amplify
frontend/public/amplify_outputs.json

# amplify
node_modules
.amplify
amplify_outputs*
amplifyconfiguration*

# npm package cache — written by the `--cache .npm` installs in amplify.yml
# and persisted by that file's `cache:` block; never checked in.
.npm/
docs/how-to/isolation.md
